server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }

    location /goals {
        proxy_pass http://localhost:5000/goals;
    }
}
